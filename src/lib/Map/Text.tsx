export interface InnerTextProps {
  color?: string;
  size?: number;
  outline?: boolean;
  children: string;
}

export default function InnerText({
  color,
  size,
  outline,
  children,
}: InnerTextProps) {
  return <></>;
}
